/** Stonepass Floor 1 temporary-instance ids (legacy world file until W5-S13). */
export const STONEPASS_HIDDEN_CAVE_INSTANCE_ID = "instance_hidden_cave";

/** Set when a consequence exposes the hidden cave for entry (W5-S1 gate). */
export const CAVE_EXPOSED_FLAG = "cave_exposed";
